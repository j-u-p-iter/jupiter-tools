import { prepareConfig } from "../../scripts/commit/prepareConfig.js";

describe("prepareConfig", () => {
  it("returns config passed in options", async () => {
    expect(true).toBe(false);
  });
});

{
  "name": "jupiter-tools",
  "version": "1.0.0",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -Apt run build",
    "test": "yarn workspaces foreach -Ap run test run"
  },
  "volta": {
    "node": "18.18.0"
  },
  "packageManager": "yarn@4.0.0"
}
